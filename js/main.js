// ===== i18n =====
const I18N = {
  'zh-CN': {
    skip: '跳到主要内容',
    brandName: '健康喝水',
    navHow: '使用流程', navFeatures: '功能特点', navScreenshots: '应用截图', navFaq: '常见问题', navGet: '获取 App',
    heroEyebrow: '✦ AI 驱动的饮水助手',
    heroTitle: '会思考的喝水助手',
    heroSubtitle: 'AI 根据你的体重、活动量和天气，算出真正适合你的每日目标，并在恰当的时刻温柔提醒。',
    downloadBtn: 'App Store 下载', learnMore: '了解更多',
    trust1: '🔒 数据全本地', trust2: '🔔 智能提醒', trust3: '🌏 中英双语',
    hydroToday: '今天', hydroAi: 'AI 已定制',
    hydroGoal: '今日目标 2100ml · 按体重与天气定制',
    hydroTip: 'AI 建议：现在补 300ml，保持节奏',
    chipA: '💧 已喝 1450ml', chipB: '🔔 下次 14:30',
    proofRating: '健康 · 生活方式',
    stat1: '个性化目标', stat2: '本地存储', stat3: '双语界面', stat4: '广告 · 追踪',
    howTitle: '三步喝对每一天', howSub: '告诉它你的情况，剩下的交给 AI。',
    step1Title: '告诉它你的情况', step1Desc: '输入体重、活动量和所在气候，一次设置，之后无需再操心。',
    step2Title: 'AI 算出你的专属目标', step2Desc: '不是千篇一律的 2000ml，而是为你量身定制、还会随天气变化的每日饮水量。',
    step3Title: '智能提醒，轻松达标', step3Desc: '在恰当时刻提醒你补水，随手一点即可记录，看着进度环一点点涨满。',
    featuresTitle: '核心功能', featuresSub: '为轻松、稳定的补水习惯而设计。',
    feature1Title: 'AI 自适应目标', feature1Desc: '根据体重、活动量和气候计算你的专属每日目标，并随进度和天气动态调整。',
    feature2Title: '智能喝水提醒', feature2Desc: '可开关的定时提醒，间隔 30 分钟到 2 小时随心选，AI 结合当天进度温柔提示。',
    feature3Title: '进度环与统计', feature3Desc: '直观的进度环，配合饮水次数、上次时间与平均每次水量，一眼看清今日状态。',
    feature4Title: '快速记录', feature4Desc: '200 / 250 / 500 / 1000ml 一键快速添加，也支持输入自定义水量，记录只需一秒。',
    feature5Title: '中英双语', feature5Desc: '完整支持简体中文与英文界面，随时一键切换，适合不同使用习惯。',
    feature6Title: '隐私优先', feature6Desc: '全部数据本地存储，无需账号、无需联网，你的饮水记录只属于你自己。',
    screenshotsTitle: '应用截图', screenshotsSub: '左右滑动，点击可放大查看。',
    scToday: '今天', scAi: 'AI 定制', scGoalNote: '目标 2100ml · 为你定制',
    scCount: '次数', scLast: '上次', scAvg: '平均 ml',
    scLogTitle: '记录饮水', scCustom: '自定义水量…', scAdd: '添加', scRecent: '今日记录',
    scRemTitle: '喝水提醒', scRemOn: '开启提醒', scRemInt: '提醒间隔',
    scI1: '30 分钟', scI2: '1 小时', scI3: '1.5 小时', scI4: '2 小时',
    scNextNote: '下次提醒 · 已喝 1450ml', scGlass: '一杯水 250ml', scGlass2: '一杯水 250ml',
    scStatsTitle: '本周统计', scWeek: '近 7 天',
    scD1: '一', scD2: '二', scD3: '三', scD4: '四', scD5: '五', scD6: '六', scD7: '日',
    scGoalDays: '达标天数', scDaily: '日均饮水',
    shot1: 'AI 定制目标与进度', shot2: '一键快速记录饮水', shot3: '可自定义的智能提醒', shot4: '每周趋势一目了然',
    privacyTitle: '你的数据，只属于你', privacyDesc: '没有账号，没有云端，没有追踪。所有饮水记录都保存在设备本地，卸载即彻底清除。',
    pp1: '数据本地存储', pp2: '离线即可使用', pp3: '无任何追踪', pp4: '无广告干扰',
    faqTitle: '常见问题',
    q1: 'AI 目标是怎么算出来的？', a1: 'AI 会结合你输入的体重、活动量和所在气候，算出比统一 2000ml 更贴合你的每日饮水目标，并随你的记录动态调整。',
    q2: '喝水数据会上传云端吗？', a2: '不会。所有数据都保存在你的设备本地，我们不上传、不收集、不追踪任何饮水信息。',
    q3: '提醒可以自定义吗？', a3: '可以。你能一键开启或关闭提醒，并选择 30 分钟到 2 小时的间隔，AI 也会结合当天进度在恰当的时刻提醒你。',
    q4: '需要联网才能使用吗？', a4: '核心的记录、目标与提醒完全离线可用，无需登录账号，卸载即彻底清除全部数据。',
    q5: '支持哪些语言和设备？', a5: '支持简体中文与英文双语界面，适用于 iPhone。',
    downloadTitle: '立即下载 健康喝水', downloadSubtitle: '让 AI 帮你养成稳定的补水习惯。', downloadNote: '适用于 iPhone · 免费下载',
    footerRights: '健康喝水 (AI Drink Water). 保留所有权利。', footerMadeBy: 'Built by WeiProduct',
    stickySub: 'AI 个性化饮水 · 免费', stickyGet: '获取'
  },
  'en': {
    skip: 'Skip to content',
    brandName: 'AI Drink Water',
    navHow: 'How it works', navFeatures: 'Features', navScreenshots: 'Screenshots', navFaq: 'FAQ', navGet: 'Get App',
    heroEyebrow: '✦ AI-powered hydration',
    heroTitle: 'Hydration that thinks for you',
    heroSubtitle: 'AI computes a daily goal that actually fits your body, activity, and weather — then nudges you at just the right moments.',
    downloadBtn: 'Download on App Store', learnMore: 'Learn more',
    trust1: '🔒 Fully on-device', trust2: '🔔 Smart reminders', trust3: '🌏 EN / 中文',
    hydroToday: 'Today', hydroAi: 'AI-tailored',
    hydroGoal: "Today's goal 2100ml · set by body & weather",
    hydroTip: 'AI tip: sip 300ml now to keep your pace',
    chipA: '💧 1450ml so far', chipB: '🔔 Next 14:30',
    proofRating: 'Health · Lifestyle',
    stat1: 'Personalized goal', stat2: 'On-device', stat3: 'Bilingual', stat4: 'Ads · Tracking',
    howTitle: 'Hydrate right in three steps', howSub: 'Tell it about you — AI handles the rest.',
    step1Title: 'Tell it about you', step1Desc: 'Enter your weight, activity level, and climate once — no more guesswork afterward.',
    step2Title: 'AI computes your goal', step2Desc: "Not a flat 2000ml — a daily target tailored to you that even adapts to the weather.",
    step3Title: 'Smart nudges keep you on track', step3Desc: 'Timely reminders to drink, one-tap logging, and a progress ring you watch fill up.',
    featuresTitle: 'Core Features', featuresSub: 'Designed for an effortless, consistent hydration habit.',
    feature1Title: 'AI Adaptive Goal', feature1Desc: 'Computes your personal daily target from weight, activity, and climate, adjusting to progress and weather.',
    feature2Title: 'Smart Reminders', feature2Desc: 'Toggleable reminders every 30 minutes to 2 hours, with AI nudges based on how much you have had today.',
    feature3Title: 'Progress Ring & Stats', feature3Desc: 'A clear progress ring plus drink count, last-drink time, and average per drink at a glance.',
    feature4Title: 'Quick Logging', feature4Desc: 'One-tap add for 200 / 250 / 500 / 1000ml, or enter a custom amount — logging takes a second.',
    feature5Title: 'Bilingual', feature5Desc: 'Full Chinese and English interfaces with one-tap switching to fit every user.',
    feature6Title: 'Privacy First', feature6Desc: 'All data stored locally, no account and no internet needed — your logs stay yours alone.',
    screenshotsTitle: 'Screenshots', screenshotsSub: 'Swipe through — tap any shot to zoom in.',
    scToday: 'Today', scAi: 'AI-set', scGoalNote: 'Goal 2100ml · tailored to you',
    scCount: 'Times', scLast: 'Last', scAvg: 'Avg ml',
    scLogTitle: 'Log Water', scCustom: 'Custom amount…', scAdd: 'Add', scRecent: 'Today',
    scRemTitle: 'Reminders', scRemOn: 'Reminders on', scRemInt: 'Interval',
    scI1: '30 min', scI2: '1 hour', scI3: '1.5 hours', scI4: '2 hours',
    scNextNote: 'Next reminder · 1450ml so far', scGlass: 'A glass · 250ml', scGlass2: 'A glass · 250ml',
    scStatsTitle: 'This Week', scWeek: 'Last 7 days',
    scD1: 'M', scD2: 'T', scD3: 'W', scD4: 'T', scD5: 'F', scD6: 'S', scD7: 'S',
    scGoalDays: 'Goal days', scDaily: 'Daily avg',
    shot1: 'AI-tailored goal & progress', shot2: 'One-tap water logging', shot3: 'Customizable smart reminders', shot4: 'See your weekly trend',
    privacyTitle: 'Your data is yours alone', privacyDesc: 'No account, no cloud, no tracking. Every log is stored on your device and wiped completely on uninstall.',
    pp1: 'Stored on-device', pp2: 'Works offline', pp3: 'Zero tracking', pp4: 'No ads',
    faqTitle: 'Frequently Asked Questions',
    q1: 'How is the AI goal calculated?', a1: 'AI combines the weight, activity level, and climate you enter to compute a daily target that fits you better than a flat 2000ml, adjusting as you log.',
    q2: 'Is my hydration data uploaded to the cloud?', a2: 'No. All data stays on your device. We never upload, collect, or track any hydration information.',
    q3: 'Can reminders be customized?', a3: 'Yes. Turn reminders on or off with one tap and pick an interval from 30 minutes to 2 hours; AI also nudges you based on the day’s progress.',
    q4: 'Do I need an internet connection?', a4: 'Core logging, goals, and reminders all work fully offline with no account, and everything is erased completely on uninstall.',
    q5: 'Which languages and devices are supported?', a5: 'Simplified Chinese and English interfaces, for iPhone.',
    downloadTitle: 'Download AI Drink Water', downloadSubtitle: 'Let AI help you build a steady hydration habit.', downloadNote: 'For iPhone · Free download',
    footerRights: 'AI Drink Water. All rights reserved.', footerMadeBy: 'Built by WeiProduct',
    stickySub: 'AI-personalized hydration · Free', stickyGet: 'Get'
  }
};

let currentLang = 'zh-CN';

function applyLang(lang) {
  currentLang = I18N[lang] ? lang : 'zh-CN';
  const t = I18N[currentLang];
  document.querySelectorAll('[data-i18n]').forEach(el => {
    const k = el.getAttribute('data-i18n');
    if (t[k] !== undefined) el.textContent = t[k];
  });
  document.documentElement.lang = currentLang;
  const ls = document.getElementById('langSwitch');
  if (ls) ls.textContent = currentLang === 'zh-CN' ? 'EN' : '中文';
  try { localStorage.setItem('lang', currentLang); } catch (e) {}
}

function initLang() {
  let saved;
  try { saved = localStorage.getItem('lang'); } catch (e) {}
  if (!saved) saved = (navigator.language || '').toLowerCase().startsWith('zh') ? 'zh-CN' : 'en';
  applyLang(saved);
}

// ===== Theme =====
function setTheme(theme) {
  document.documentElement.setAttribute('data-theme', theme);
  const meta = document.getElementById('themeColorMeta');
  if (meta) meta.setAttribute('content', theme === 'dark' ? '#0C121A' : '#4A90E2');
  try { localStorage.setItem('theme', theme); } catch (e) {}
}
function initTheme() {
  let saved;
  try { saved = localStorage.getItem('theme'); } catch (e) {}
  if (!saved) saved = window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
  setTheme(saved);
}

// ===== Gallery =====
function initGallery() {
  const track = document.getElementById('galTrack');
  if (!track) return;
  const shots = Array.from(track.children);
  const dotsWrap = document.getElementById('galDots');
  const prev = document.getElementById('galPrev');
  const next = document.getElementById('galNext');

  shots.forEach((_, i) => {
    const b = document.createElement('button');
    b.type = 'button';
    b.setAttribute('aria-label', 'screenshot ' + (i + 1));
    if (i === 0) b.classList.add('active');
    b.addEventListener('click', () => shots[i].scrollIntoView({ behavior: 'smooth', inline: 'center', block: 'nearest' }));
    dotsWrap.appendChild(b);
  });
  const dots = Array.from(dotsWrap.children);

  function activeIndex() {
    const c = track.scrollLeft + track.clientWidth / 2;
    let best = 0, bd = Infinity;
    shots.forEach((s, i) => {
      const center = s.offsetLeft + s.offsetWidth / 2;
      const d = Math.abs(center - c);
      if (d < bd) { bd = d; best = i; }
    });
    return best;
  }
  track.addEventListener('scroll', () => {
    const i = activeIndex();
    dots.forEach((d, j) => d.classList.toggle('active', j === i));
  }, { passive: true });

  const step = () => (shots[1] ? shots[1].offsetLeft - shots[0].offsetLeft : 300);
  if (prev) prev.addEventListener('click', () => track.scrollBy({ left: -step(), behavior: 'smooth' }));
  if (next) next.addEventListener('click', () => track.scrollBy({ left: step(), behavior: 'smooth' }));

  // Lightbox — clones the CSS phone mockup (no external image assets)
  const lb = document.createElement('div');
  lb.className = 'lightbox';
  lb.innerHTML = '<button class="lightbox-close" aria-label="close">&times;</button>';
  document.body.appendChild(lb);
  const close = () => {
    lb.classList.remove('open');
    const p = lb.querySelector('.phone');
    if (p) p.remove();
  };
  lb.addEventListener('click', e => { if (e.target === lb || e.target.classList.contains('lightbox-close')) close(); });
  document.addEventListener('keydown', e => { if (e.key === 'Escape') close(); });
  track.querySelectorAll('.phone').forEach(p => {
    p.addEventListener('click', () => {
      const existing = lb.querySelector('.phone');
      if (existing) existing.remove();
      lb.appendChild(p.cloneNode(true));
      lb.classList.add('open');
    });
  });
}

// ===== Scroll reveal =====
function initReveal() {
  const els = document.querySelectorAll('.reveal');
  if (!('IntersectionObserver' in window)) { els.forEach(e => e.classList.add('in')); return; }
  const io = new IntersectionObserver((entries) => {
    entries.forEach(en => {
      if (en.isIntersecting) { en.target.classList.add('in'); io.unobserve(en.target); }
    });
  }, { threshold: 0.12, rootMargin: '0px 0px -40px 0px' });
  els.forEach((el, i) => { el.style.transitionDelay = (Math.min(i, 6) * 0.05) + 's'; io.observe(el); });
}

// ===== Nav + sticky =====
function initScroll() {
  const nav = document.getElementById('navbar');
  const sticky = document.getElementById('stickyCta');
  const onScroll = () => {
    const y = window.scrollY;
    if (nav) nav.classList.toggle('scrolled', y > 20);
    if (sticky) sticky.classList.toggle('show', y > 620);
  };
  window.addEventListener('scroll', onScroll, { passive: true });
  onScroll();
}

// ===== Init =====
document.addEventListener('DOMContentLoaded', () => {
  initTheme();
  initLang();
  initGallery();
  initReveal();
  initScroll();

  const yr = document.getElementById('currentYear');
  if (yr) yr.textContent = new Date().getFullYear();

  const ls = document.getElementById('langSwitch');
  if (ls) ls.addEventListener('click', () => applyLang(currentLang === 'zh-CN' ? 'en' : 'zh-CN'));
  const tt = document.getElementById('themeToggle');
  if (tt) tt.addEventListener('click', () => setTheme(document.documentElement.getAttribute('data-theme') === 'dark' ? 'light' : 'dark'));

  document.querySelectorAll('a[href^="#"]').forEach(a => {
    a.addEventListener('click', function (e) {
      const id = this.getAttribute('href');
      if (id.length > 1) {
        const target = document.querySelector(id);
        if (target) { e.preventDefault(); target.scrollIntoView({ behavior: 'smooth', block: 'start' }); }
      }
    });
  });
});
